spring:
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        prefix: sbc-logistics
        file-extension: yaml
        group: DEFAULT_GROUP
        enabled: true
        namespace:
      discovery:
        namespace:
        server-addr: 127.0.0.1:8848
  profiles:
    active: local
